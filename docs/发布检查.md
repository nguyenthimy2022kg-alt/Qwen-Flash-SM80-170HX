# 发布检查

[返回首页](../README.md) · [部署指南](从零部署.md) · [性能记录](性能记录.md)

日期：2026-09-08。以下按时间顺序保留各阶段的构建、部署及验证结果；镜像标签、测试数量和服务状态均对应各节所述阶段。

**当前版本：v0.1.4。** 部署工具、构建文件排除规则和中英文使用流程已完成本轮检查，23 项 CPU 测试通过。推理源码与已发布运行版本保持一致；既有 8K～128K 单请求成绩见 [性能记录](性能记录.md)。完整质量、满上下文、多请求并发及第二台机器的从零部署仍未完成验收。

## 初始打包检查

- Dockerfile 构建完成，固定上游校验通过，两个 GDS C++ 扩展成功编译。构建使用此前缓存且与 PyPI 官方 SHA256 相同的 wheel，经本机临时索引安装；没有重新完成一次全量公网下载。
- 新镜像中 11 个模块导入检查通过，含两个本地读取扩展和主要运行适配。框架导入会初始化 CUDA 上下文，但两卡 PyTorch 张量分配均为 0 字节；没有加载模型。
- Python 源码语法检查通过；构建脚本 Bash 语法检查通过。
- 六项 CPU 测试通过：启动参数与双卡分工、关闭 INT8 的原路径回退、同一显卡别名误选拦截、不匹配上游时拒绝覆盖、PLE 数据损坏校验、377 个预加载内核 SHA256。
- 从本机真实检查点仅读取文件头，成功生成 128 分片映射；已有 PLE 元数据成功登记。该检查仅覆盖文件头与元数据。
- 最新草稿 `get_top_tokens` 计算函数经 AST 对比保持相同；HC、TileLang、INT8 量化内核及相应运行适配中的七个文件与已选定版本逐字节相同。
- 发布版将数据身份、模型路径、显卡、端口、可选历史样本检查改为配置，保留核心计算和其他形状回退。
- 真实检查点的 128 分片映射与本机完整行数 320001536 的 PLE 身份配置校验通过；只读取文件头/元数据。
- 发布目录检查未发现本机用户名路径、真实显卡 UUID 或 GitHub 令牌；模型权重、真实输入样本、容器日志未加入仓库。

此阶段尚未加载完整模型或复测长输出；170.826 tok/s 为打包前运行版本的历史记录。后续服务测速见本页顶部的当前状态。

## v0.1.1 启动修复

v0.1.0 首次实际启动暴露了 PLE 身份配置读取遗漏 `import json` 的错误，服务在权重加载前退出。v0.1.1 补齐导入，并在修正镜像中实际调用 `_validate_artifact`，当前 320001536 行 PLE 数据的元数据与身份校验通过。该修复不改变数值计算。

修正镜像已完成模型加载、编译及预热，`/health` 与 `/v1/models` 均返回 HTTP 200。启动检查未发送生成请求；长时间稳定性与并发验证尚未完成。

## v0.1.2 部署准备检查

- 固定公开模型来源为 RadixArk/Qwen3.8-Flash-Next-NVFP4，revision `7b719225242aacd3dbd3f9407468c2ee9a9d2594`。匿名下载 config/index 成功，两文件与已运行检查点逐字节一致。没有重新下载全部约 135 GB 权重。
- 新增 `scripts/check-model.py`，仅使用标准库检查模型版本、分片文件头、PLE 和 MTP 格式。当前检查点 206 个 safetensors 文件、31 个 BF16 MTP 张量和 320001536 行 PLE 检查通过；未重读所有权重内容计算 SHA256。
- 真实 PLE 前 257 行完成映射、跨 256 行块边界转换和完整输出数据校验；未重复转换整张 PLE 表。
- CPU 回归测试增至 10 项，新增未完成下载、错误配置版本及错误 MTP dtype 的拦截检查。
- 新增从零部署说明，直接使用公开检查点，不依赖历史备份文件或本机视图。

这些检查不等同于另一台机器的从零整模型部署，也不替代稳定性或性能测试。

## 独立构建与当前服务一致性

2026-09-08 独立克隆目录完成无缓存构建：从 NVIDIA 下载 cuFile 开发包，从 PyPI 下载带固定 SHA256 的 TileLang/TVM-FFI，编译两个 GDS 扩展并安装 29 个增量 Python 文件。使用已缓存的固定公开基础镜像层，未重新下载基础镜像全部层。新增部署工具合入后，再构建 `qwen-flash-sm80:0.1.2` 镜像。

新镜像内实际导入两个 GDS 扩展并调用 `_validate_artifact` 通过，校验 320001536 行 PLE 元数据；本次检查未初始化 CUDA 上下文。GitHub CPU 检查通过 10 项测试。

与仍在运行的 `qwen-flash-sm80:0.1.1` 服务比较：

| 检查项 | 结果 |
|---|---|
| 发布源码与预加载内核 | 407 个文件 SHA256 全部一致 |
| GDS 与 vLLM 原生扩展 | 19 个 `.so` 文件 SHA256 全部一致 |
| 主要依赖 | vLLM、PyTorch、Triton、TileLang、TVM-FFI、FlashInfer 版本一致 |
| 启动参数 | 发布启动器代入当前部署配置后，与服务参数一致 |
| 运行环境设置 | 38 项一致 |
| 模型 config/index | 与固定公开 revision 下载文件逐字节一致 |
| 运行中服务 | `/health` 返回 HTTP 200，未重启 |

版本号差异来自部署工具、配置示例和文档，不代表更换推理实现。模型路径、GPU 顺序等仍由每台机器的部署配置决定；本次未导出显存权重或重新执行整模型生成测试。

驱动层不在上述镜像一致性范围内。参考机器使用本地 CMP v0.3 overlay，其实际配置见 [CMP P2P 部署参考](CMP_P2P.md)；该 overlay 未随仓库分发，外部 cmpunlocker 默认版本不等同于参考机器驱动。

部署要求以 [主机验收条件](从零部署.md#主机验收条件) 为准。上述驱动来源差异用于记录参考环境，不要求其他机器复制本地 overlay；满足运行条件即可继续部署，性能由目标硬件实测。

## v0.1.3 发布收尾

- 启动前拦截未填写的必填路径、把配置文件误设为目录等错误；显卡编号或 UUID 不存在时给出明确提示。
- PLE 映射增加全局缩放值必须为单个 BF16 值的检查，避免到后续转换阶段才发现格式错误。
- 映射和身份登记在读取数据前检查输出文件是否已存在，避免重复登记失败前白读约 51.2 GB 数据；仍禁止覆盖已有输出。
- CPU 回归检查共 14 项通过；本机真实 128 分片映射、全局缩放值和启动路径检查通过。
- 使用本机配置生成的启动命令与当前服务保存的命令逐项一致。`src/`、运行参数模板及 Dockerfile 未修改，未重新构建镜像或重跑整模型生成测试。

此版本仅修补部署准备工具并更新文档；当前已加载模型的服务继续使用原镜像。v0.1.3 镜像标签用于从此版本源码自行构建，没有发布预构建镜像。

## v0.1.4 使用流程与部署检查

- 中英文首页增加阅读导航和分阶段部署入口，保留完整优化表；统一配置、API、SSH 接入和常见故障说明。
- P2P/GDS 指南明确主机准备范围、已确认的参考拓扑和读盘卡顺序；按实际输出区分平台能力与传输校验。
- 配置错误现在明确指出字段；拒绝非整数端口、无效 GPU 数组和非有限内存阈值。后台启动前检查 Docker、本地镜像与容器名称冲突。
- 模型检查拒绝损坏的张量位置、形状和未知数据类型；停止服务只针对已核对项目标签的容器 ID。
- 构建排除本地环境文件、权重、样本及本机编译产物。使用不含真实数据的最小 Docker 构建验证：9 项本地文件被排除，源码、内核文件和公开配置仍保留。
- `qwen-flash-sm80:0.1.4` 镜像构建通过，复用固定基础镜像和依赖缓存；两个 GDS 扩展重新编译成功。镜像内 23 项 CPU 测试通过，407 个运行源码与预加载文件 SHA256 与发布目录一致，本地配置未打入镜像。
- 14 份 Markdown 的 111 处内部链接、71 个 Bash 示例和 2 个 JSON 示例检查通过。
- 23 项 CPU 测试、Python/Bash 语法检查通过。本机真实检查点的 206 个分片文件头及当前配置、路径、Docker/镜像前置检查通过；没有重新读取全部权重内容或生成请求。

此次修改不改变 `src/`、推理参数、内核依赖或模型权重。镜像标签用于从对应源码自行构建；仓库不提供预构建镜像。运行性能沿用已有测试记录，不将部署工具修复记为推理提速。
